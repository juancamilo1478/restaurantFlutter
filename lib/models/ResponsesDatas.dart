class Propines {
  String name;
  num propine;
  Propines(this.name, this.propine);
}

class PropinesCard {
  num total;
  PropinesCard(this.total);
}

class pool {
  num id;
  String name;
  num price;
  num quantity;
  pool(this.id, this.name, this.price, this.quantity);
}
